[package]
name = "das-dynamic-libs"
version = "1.0.0"
edition = "2021"

[features]
default = ["dev"]
dev = []
local = []
testnet = []
mainnet = []

[dependencies]
ckb-std = { workspace = true }
blake2b-ref = "0.3"
das-types = { workspace = true, default-features = false }
