[package]
name = "das-map"
version = "1.0.0"
edition = "2021"

[features]
default = ["no_std"]
std = ["no-std-compat/std"]
no_std = []

[dependencies]
hex = { workspace = true }
no-std-compat = { workspace = true }

